<UserControl x:Class="Vex.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Vex"
             mc:Ignorable="d" 
             d:DesignHeight="510" d:DesignWidth="781">
    <Grid>
        <Rectangle IsHitTestVisible="True" Opacity="0.95"/>
        <StackPanel Orientation="Vertical" Height="105" Width="500" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ProgressBar x:Name="ProgressBar" Height="50"/>
            <Viewbox Height="50" Width="450">
                <TextBlock x:Name="ProgressText" Text="LOADING" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </Viewbox>
        </StackPanel>
    </Grid>
</UserControl>
